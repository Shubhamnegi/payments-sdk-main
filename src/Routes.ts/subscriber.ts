import { Router } from 'express'

export const subscriberRoutes = Router();

/**
 * Responsible to create new subscriber
 * Manage authentication of subscriber
 * Share details of subscriber
 */

subscriberRoutes.post("/v1/create", (req, res, next) => {

})
