import { Router } from 'express'

export const businessUnitRoutes = Router()

/**
 * Responsible to manage business unit
 * Create new business unit
 */
businessUnitRoutes.post("/v1/create", (req, res, next) => {

})
