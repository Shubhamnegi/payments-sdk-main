import { Router } from 'express'

export const integrationRoute = Router()

/**
 * Resonsible to manage Integration
 * Add new integration to a business unit
 * Edit integration in business unit
 */
integrationRoute.post("/v1/create", (req, res, next) => {

})
