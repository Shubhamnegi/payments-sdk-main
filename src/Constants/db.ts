export const DbConstants = {
    USERNAME: process.env.USERNAME || 'root',
    PASSWORD: process.env.PASSWORD || 'root',
    DATABASE: process.env.DATABASE || 'payserv_sdk',
    HOST: process.env.HOST || "localhost"
}